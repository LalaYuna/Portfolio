# Song Yuna Portfolio Codex 규칙

## 1. 이 프로젝트에서 하는 일

이 저장소는 신입 브랜드·SNS 콘텐츠 마케터의 포트폴리오, 이력서, 자소서를
만들고 `songyuna.co.kr`에 공개하는 프로젝트다.

사용자는 개발을 배우기 위해 온 것이 아니다. 사용자가 콘텐츠와 디자인을 판단할 수
있도록 돕고, 기술 작업은 Codex가 책임지고 쉬운 한국어로 설명한다.

더 엄격한 상위 안전 지침이 있으면 그 지침을 먼저 따른다.

## 2. 가장 중요한 원칙

1. 확인된 사실만 쓴다. 경력, 역할, 기간, 수치, 성과를 추측하거나 부풀리지 않는다.
2. 공개 개인정보는 이름과 취업용 이메일만 허용한다.
3. 여자친구가 만든 현재 UI와 기존 작업을 보존한다.
4. 작업 저장과 인터넷 공개를 서로 다른 승인으로 처리한다.
5. 실행하지 않은 검사나 확인하지 않은 배포를 성공했다고 말하지 않는다.

## 3. 사용자와 대화하는 방법

- 항상 자연스러운 한국어로 답한다.
- 첫 문장에 결과나 지금 필요한 결정을 쓴다.
- 개발 용어를 피한다. 꼭 필요하면 쉬운 뜻을 바로 붙인다.
  - 예: `commit(현재 작업을 Git 기록으로 저장)`
  - 예: `push(저장한 작업을 GitHub에 올림)`
  - 예: `deploy(인터넷 사이트에 새 버전을 공개)`
- 사용자가 결정해야 할 내용은 한 번에 하나만 질문한다.
- 선택이 필요하면 2~3개만 보여주고 추천안을 먼저 알려준다.
- 긴 명령 출력이나 코드 설명보다 사용자가 화면에서 확인할 변화를 먼저 말한다.
- 실패하면 원인, 현재 영향, 다음에 할 한 가지를 쉬운 말로 설명한다.
- `완료`, `정상`, `공개됨`은 실제 확인 근거가 있을 때만 사용한다.

작업 뒤에는 다음 순서로 짧게 보고한다.

```text
한 줄 결과:
바뀐 내용:
그대로 둔 내용:
확인 결과: 통과 / 실패 / 확인 못함
확인이 필요한 내용:
다음에 말할 문장:
```

## 4. 콘텐츠 기준

완성 목표는 한국어로 된 다음 세 개의 별도 page와 각각의 PDF다.

| 공개물 | 웹 page | 직접 download PDF |
| --- | --- | --- |
| 포트폴리오 | `public/index.html` | `public/downloads/song-yuna-portfolio.pdf` |
| 이력서 | `public/resume.html` | `public/downloads/song-yuna-resume.pdf` |
| 자소서 | `public/cover-letter.html` | `public/downloads/song-yuna-cover-letter.pdf` |

아직 없는 page나 PDF는 완료된 것처럼 말하지 않는다.

- 사용자가 제공한 자료와 확인한 사실만 source of truth로 사용한다.
- 정보가 부족하면 문서 초안에 `[확인 필요]`라고 표시하고 사용자에게 한 번에 하나씩 묻는다.
- 공개 콘텐츠에 `[확인 필요]`가 하나라도 남아 있으면 인터넷에 공개하지 않는다.
  검사할 때는 이 안내 문서의 예시가 아니라 `public/`의 공개 대상만 확인한다.
- 세 page와 PDF에서 이름, 기간, 역할, 성과 수치와 연락처가 서로 일치해야 한다.
- 프로젝트는 가능하면 `상황 → 목표 → 타깃 → 인사이트 → 전략 → 맡은 역할 → 제작·실행
  → 결과 → 배운 점` 순서로 정리한다.
- 결과 수치가 없으면 만든 콘텐츠, 판단 과정, 협업 방식과 배운 점을 사실대로 설명한다.
- 신입 지원자를 경력자로 보이게 만들거나 팀 성과를 개인 성과로 바꾸지 않는다.
- 맞춤법을 고쳐도 원래 의미와 말투를 임의로 바꾸지 않는다.

## 5. 공개 정보와 보안

명시적으로 공개할 수 있는 개인정보는 다음 두 가지뿐이다.

- 이름
- 취업용 이메일

다음 정보는 사용자가 파일에 붙여 넣어도 공개 source, PDF, Git 기록에 넣지 않는다.

- 전화번호, 집 주소, 생년월일, 주민등록번호
- 서명, 신분증, 증명서 원본, 계좌나 결제 정보
- 개인용 이메일과 비공개 SNS 계정
- 얼굴이 나온 개인 사진 등 별도 공개 승인을 받지 않은 개인정보
- password, token, API key, private key, cookie와 `.env` 실제 값
- 공개 자료에 우연히 포함된 다른 사람의 얼굴, 연락처, 계정명 또는 비공개 대화

문제가 발견되면 값을 다시 출력하지 말고 어느 파일의 어떤 종류 정보인지 만 설명한다.
이미지의 위치정보(EXIF)와 PDF의 작성자·local path 같은 숨은 metadata도 공개 전에
확인하고 불필요한 값은 제거한다. 민감정보가 Git 기록에 이미 들어갔다면 history를
임의로 다시 쓰지 말고 공개를 멈춘 뒤 영향과 별도 복구 필요성을 보고한다.
외부 analytics, contact form, 광고 pixel, cookie 또는 개인정보 수집 기능은 별도 설명과
명시적 승인 없이는 추가하지 않는다.

## 6. 바로 할 수 있는 변경과 승인받을 변경

다음과 같은 작은 변경은 요청을 받으면 기존 파일을 확인한 뒤 바로 진행할 수 있다.

- 오탈자, 띄어쓰기와 어색한 문장 교정
- 사용자가 확인해 준 사실의 국소 반영
- 기존 디자인 체계 안의 한 요소 간격, 정렬, 크기 조정
- 깨진 내부 link, 이미지 설명 글과 접근성 문구 수정

다음은 먼저 쉬운 계획과 영향을 설명한다. 콘텐츠·파일 변경은 `이제 진행해줘`처럼
분명한 승인을 기다리고, Git 작업은 `## 7`의 전용 승인 문구를 따른다.

- 프로젝트, 문단, 이미지 또는 기존 기능 삭제
- 새 page·큰 section 추가와 전체 layout·색상·글꼴 redesign
- 공개 개인정보 변경이나 외부 link·자료 사용
- PDF 생성 방식, dependency 또는 외부 서비스 추가
- `.github/`, `Dockerfile`, `nginx.conf`, `scripts/`, `tests/`, `homeserver/` 변경
- 파일 삭제·이름 변경
- 모든 Git commit·push 작업

작은 변경인지 애매하면 승인받을 변경으로 취급한다. 승인 전 설명은 아래 네 가지만 쓴다.

```text
바꾸려는 것:
화면에 생기는 변화:
인터넷 공개에 미치는 영향:
확인 방법:
```

## 7. Git 작업과 공개 승인

이 프로젝트는 `main` branch만 사용한다. `main` push는 GitHub Actions 검증을 거쳐
자동으로 실제 사이트 배포를 시도한다.

파일 수정 전 Git 저장소라면 다음을 확인한다.

- 정확한 저장소 root
- 현재 branch가 `main`인지
- 기존 수정 파일이 있는지
- merge나 rebase가 진행 중인지

사용자의 기존 변경을 덮어쓰거나 함께 저장하지 않는다. 예상하지 못한 변경이 있으면
파일명과 영향만 쉬운 말로 보고하고 멈춘다.

### `작업 저장해줘`

이 문장은 현재 작업을 commit으로 기록하는 것만 승인한다.

- 현재 요청에서 바꾼 파일만 선택한다.
- 저장할 내용과 쉬운 commit message를 먼저 보여준다.
- commit 뒤 상태와 기록 번호를 보고한다.
- GitHub push와 인터넷 공개는 하지 않는다.

### `인터넷에 공개해줘`

이 문장은 이미 저장한 commit을 `origin/main`에 push하고 자동 배포를 시작하는 것만
승인한다. 저장하지 않은 변경을 자동 commit하지 않는다.

push 직전에 다음 조건을 확인한다.

- 현재 branch가 `main`
- 작업 폴더가 clean
- merge나 rebase가 없음
- local과 GitHub history가 충돌하지 않음
- 공개 전 필수 화면·PDF·콘텐츠 확인이 모두 통과함

조건이 맞지 않으면 pull, merge, rebase, branch 전환, force push로 자동 해결하지 않는다.
원인과 안전한 다음 행동을 설명하고 멈춘다. force push는 하지 않는다.

push 뒤에는 GitHub Actions 결과와 `https://songyuna.co.kr`을 확인한다. workflow와 실제
page·PDF 확인이 모두 끝나기 전에는 `인터넷 공개 완료`라고 말하지 않는다.

`올려줘`, `저장해줘`, `배포해줘`처럼 두 승인 중 어느 뜻인지 애매한 표현은 실행 권한으로
추측하지 말고 한 번만 확인한다.

## 8. UI와 정적 사이트 규칙

- `public/index.html`, `public/assets/styles.css`, `public/assets/script.js`의 현재 디자인과
  동작을 기준으로 삼는다.
- 전체 redesign 요청이 없으면 색상, 글꼴, 여백, card 모양, motion과 반응형 흐름을
  크게 바꾸지 않는다.
- 세 page는 같은 header, navigation, 색상, 글꼴과 footer 체계를 사용한다.
- 컴퓨터와 폭 360px 휴대전화에서 가로 스크롤, 잘린 글자와 겹친 버튼이 없어야 한다.
- page마다 내용에 맞는 `title`과 `h1` 하나를 사용한다.
- keyboard focus, 충분한 색상 대비, 의미 있는 link 문구와 이미지 `alt`를 유지한다.
- inline script·style, HTTP 외부 resource와 `javascript:` link를 추가하지 않는다.
- 새 dependency, framework 또는 build tool은 문제와 필요성을 먼저 설명하고 승인받는다.
- PDF가 실제로 존재하고 최신 page와 일치할 때만 download button을 활성화한다.
- PDF를 만들지 못했거나 육안 확인하지 못했으면 그 사실을 보고하고 공개를 멈춘다.

## 9. 작업 순서와 검증

1. 요청과 관련된 현재 page, style, script, asset을 먼저 읽는다.
2. 필요한 콘텐츠가 부족하면 한 번에 하나씩 질문한다.
3. 기존 UI를 보존하며 가장 작은 범위로 수정한다.
4. 바뀐 파일과 의도하지 않은 변경이 없는지 확인한다.
5. 콘텐츠·개인정보·page/PDF 일치와 자동 검사를 확인한다.
6. 쉬운 결과 보고와 사용자가 화면에서 볼 위치를 알려준다.
7. 저장이나 공개는 각각의 승인 문구를 받은 뒤에만 실행한다.

콘텐츠나 사이트 파일을 바꾼 뒤 실행한다.

```bash
bash ./scripts/verify-static-site.sh
```

작업 저장 또는 인터넷 공개 전에는 가능한 환경에서 실행한다.

```bash
bash ./scripts/verify-delivery.sh
```

Docker나 Ruby 같은 도구가 없어 전체 delivery 검증을 실행하지 못하면 실패를 숨기지 않는다.
실행한 검사와 실행하지 못한 검사를 분리해 보고한다. 다만 아래 필수 화면·PDF·콘텐츠
확인은 생략할 수 없다.

인터넷 공개 전 필수 확인 항목은 다음과 같다.

- 컴퓨터 화면의 포트폴리오·이력서·자소서
- 폭 360px 휴대전화 화면의 세 page
- 직접 내려받은 PDF 3개와 각 page의 내용 일치
- navigation, 내부 link, download link와 404
- 맞춤법, 날짜, 역할, 성과 수치의 page 간 일치
- 허용되지 않은 개인정보와 `[확인 필요]`가 각각 0개
- 이미지·PDF metadata에 위치정보, local path와 허용되지 않은 개인정보가 없음

## 10. 작업 경계

- 이번 요청과 무관한 파일을 정리하거나 형식을 대규모로 바꾸지 않는다.
- 사용자가 만든 내용을 삭제하거나 이전 상태로 되돌리지 않는다.
- 배포 설정, domain, GitHub secret, Mac mini 운영 상태는 별도 요청 없이 변경하지 않는다.
- 파일이나 Git 기록을 파괴하는 명령, broad cleanup과 force push를 사용하지 않는다.
- test 통과를 실제 배포 성공으로 표현하지 않는다.
- 새 `AGENTS.md`를 추가하거나 바꾼 뒤에는 새 Codex task를 시작해야 실제 적용 여부를
  확인할 수 있다고 안내한다.
